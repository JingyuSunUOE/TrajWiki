# Required by the rebuttal answer-level experiment.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Required when Qwen model files are not already available in the shared cache.
HF_TOKEN=

# Optional providers used by other benchmark configurations.
GEMINI_API_KEY=
